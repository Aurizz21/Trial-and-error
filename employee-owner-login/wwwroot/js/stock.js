document.addEventListener('DOMContentLoaded', () => { const search = document.getElementById('stockSearch'); const category = document.getElementById('stockCategory'); const rows = [...document.querySelectorAll('#stockRows tr')]; const chips = [...document.querySelectorAll('.status-filters .chip')]; let status = 'All'; const filter = () => { const query = search.value.toLowerCase(); rows.forEach(row => { row.hidden = !((!query || row.dataset.name.toLowerCase().includes(query)) && (category.value === 'All' || row.dataset.category === category.value) && (status === 'All' || row.dataset.status === status)); }); }; search?.addEventListener('input', filter); category?.addEventListener('change', filter); chips.forEach(chip => chip.addEventListener('click', () => { status = chip.dataset.status; chips.forEach(item => item.classList.toggle('active', item === chip)); filter(); })); });